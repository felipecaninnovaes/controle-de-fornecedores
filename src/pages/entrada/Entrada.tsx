import { Typography } from '@mui/material'
import { ListingTools } from '../../shared/components'
import { BaseLayoutFromPages } from '../../shared/layouts'

export const Entrada = () => {
  return (
    <BaseLayoutFromPages>
      <ListingTools
        showTitle={true}
        textTitle='Pagina de Entrada (Em desenvolvimento)'
      />

    </BaseLayoutFromPages>
  )
}
