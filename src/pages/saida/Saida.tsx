import { Typography } from '@mui/material'
import { ListingTools } from '../../shared/components'
import { BaseLayoutFromPages } from '../../shared/layouts'

export const Saida = () => {
  return (
    <BaseLayoutFromPages>
      <ListingTools
        showTitle={true}
        textTitle='Pagina de Saida (Em desenvolvimento)'
      />

    </BaseLayoutFromPages>
  )
}
